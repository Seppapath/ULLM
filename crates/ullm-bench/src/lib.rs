// SPDX-License-Identifier: Apache-2.0
//! Shared bench utilities. See `benches/*.rs` for the actual Criterion targets.
//! Each bench file is intentionally small and focused on one primitive so the
//! Criterion HTML report stays readable.

pub mod fixtures;
